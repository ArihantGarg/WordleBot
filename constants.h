#define MAX_GUESS_LIMIT 6 // Must be set to expected guess max amount
#define ANSWER_WORD "" // Leave as "" if you want it to be chosen randomly, else if you want to check a particular word then set it to that word (will throw exception if the word does not exist in the list)
#define ANSWER_WORD_FILE_PATH "./possibleAnswerWordList.txt" // Set to answer word file path
#define NON_ANSWER_WORD_FILE_PATH "./possibleNonAnswerWordList.txt" // Set to non answer word file path
